.DS_Store
__pycache__/
*.pyc

# Environment / secrets (API keys — NEVER commit)
.env
.env.local

# Raw downloads and intermediate files (regenerated by setup_sightings.sh)
data/raw/
data/*.xlsx

# Keep committed data files (sightings_map_data.json, us_population_density.json, military_bases.json)
