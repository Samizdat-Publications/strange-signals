.DS_Store
__pycache__/
*.pyc

# Environment / secrets (API keys — NEVER commit)
.env
.env.local

# Sighting data (generated by build scripts -- run setup_sightings.sh)
data/raw/
data/*.xlsx
data/sightings_map_data.json
data/us_population_density.json

# Keep curated overlay datasets (military_bases.json etc.)
